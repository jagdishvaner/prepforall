apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: prepforall
  labels:
    app.kubernetes.io/name: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: api
